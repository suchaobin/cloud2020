server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:1111 # 配置 Nacos 地址

management:
  endpoints:
    web:
      exposure:
        include: '*'